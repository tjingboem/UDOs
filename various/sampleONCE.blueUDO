<udo>
  <opcodeName>sampleONCE</opcodeName>
  <outTypes>i</outTypes>
  <inTypes>S</inTypes>
  <codeBody>Sfile xin

itable	chnget	Sfile
if (itable == 0) then
    itable   ftgen 0, 0, 0, 1, Sfile, 0, 0, 0
	chnset	itable, Sfile
endif

    xout    itable</codeBody>
  <comments>once a sample is loaded and assigned a table number, stich to that table number instead of re-load it for every note.
Mono sample version.</comments>
</udo>